package NutrientsCoders.main_project.Analysis.service;

import NutrientsCoders.main_project.dailymeal.entity.Analysis;
import NutrientsCoders.main_project.dailymeal.entity.DailyMeal;
import org.springframework.stereotype.Service;

@Service
public class AnalysisService {
  public Analysis createAnalysis(DailyMeal dailyMeal, long memberId) {
  }
  
  public Analysis findByAnalysis(long memberId, long analysisId) {
  }
  
  public void deleteAnalysis(long analysisId, long memberId) {
  }
}
